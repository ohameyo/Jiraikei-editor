# 滤镜参数与质感调教

## 优先级
P1。

## 背景
滤镜效果需要保持地雷系低饱和、病弱、冷调、少女感风格，同时避免参数过重导致脸部、手部、黑发、红色元素或低饱和区域出现脏色、色块和平台差异。本任务只负责滤镜效果、参数审美与色彩安全边界。

## 范围
- 原图、蕾粉、水色、灰粉、黑白等滤镜预设参数。
- 亮度、对比度、饱和度、色温、色调偏移、褪色/雾感的默认值、范围和联动质感。
- 肤色提白、腮红强度、黑发保护的默认值与可调范围。
- HSL 分区调色的默认值、可调范围、通道权重和保护逻辑。
- 避免脸部、手部、低饱和区域出现绿块、粉块、灰脏或断层。
- 保持移动端和 PC 端滤镜效果一致。

## 不处理
- 滑杆拖动性能、预览降采样、缓存、worker、分块渲染。
- 导出流程和导出画质策略。
- 文字、贴纸、马赛克图层效果。
- 腮红选区交互、手动选区体验和识别失败流程。

## 当前问题
- 水色、蕾粉、HSL 在部分照片上可能让脸部或手部出现绿/粉色块。
- 色相、亮度、饱和度范围过大时容易破坏肤色和低饱和区域。
- 黑发、深色衣物和红色小物容易被滤镜拉脏、偏灰或丢失识别度。
- PC 与移动端若采用不同预览路径，最终视觉需要保持同一套参数结果。
- 预设滤镜需要更明确的审美目标，避免只是简单偏色。
- 部分图片在黑白、水色、灰粉等低饱和底色下，腮红强度调节不够明显。
- PC 端 HSL 默认选中绿通道，用户直接拖动滑杆时只影响绿色区域，在多数人像图上容易表现为“调色无效”。
- 腮红边缘羽化不足时，强化后容易出现突兀粉块。
- 蕾粉滤镜下鼻翼、唇边等肤色微阴影区域可能出现零星绿色色块。
- 绿色色块修正曾引用局部变量作用域外的亮度值，触发时会中断滤镜渲染，表现为滤镜应用失效。
- 拖动滤镜参数实时渲染时，可见 canvas 不能先清空或改尺寸再等待滤镜计算，否则会出现灰色闪烁。
- 滤镜滑杆拖动中不能使用低清降采样或省略滤镜步骤的“临时预览”，否则会出现发灰、发糊、松手后才变清晰的问题；实时帧必须等同最终预览质感。
- 替换滤镜叠加主色需要作为一个完整可撤销步骤，不能在原生拾色器连续输入时写入大量近似历史，导致点击返回看起来无效。
- 预设强度/叠加强度降低时，所有预设参数都必须一起按比例回退到原图默认值，尤其 HSL 分区调色不能残留强去色，否则会出现强度越低反而越灰、越不像原图的问题。

## 相关文件
- `src/app.js`
- `src/config/filterPresets.js`

## 关键代码/变量
- `FILTER_CONTROL_DEFS`
- `FILTER_PRESETS`
- `HSL_CHANNELS`
- `HSL_AXES`
- `applySelectiveHsl()`
- `getHueChannelWeight()`
- `skinMask`
- `personMaskCanvas`
- `renderToneBaseCanvas()`

## 参数调教方向
- 原图：保持接近真实，仅允许轻微提亮和柔化，不主动制造明显风格偏色。
- 蕾粉：低饱和粉紫感，提亮肤色但保留红色细节，避免脸颊、手部和背景低饱和区域出现粉色块。
- 水色：冷调透明感，压低黄绿脏色，保护肤色和黑发，避免脸部泛青或手部泛绿。
- 灰粉：低对比、微雾感、轻粉灰，保留少女感但不把黑发和深色衣物洗成灰片。
- 黑白：保持柔和病弱感，控制对比不过硬，保护面部层次和头发暗部。
- HSL：默认值应克制，滑杆范围优先服务局部微调，色相和亮度范围需要比饱和度更谨慎。

## 色彩保护要求
- 肤色区域减少 HSL 色相与亮度影响，允许轻微提白和降黄，但不允许明显变绿、变紫或色块化。
- 手部应纳入肤色保护，即使未被人像主体 mask 完整覆盖，也不能出现局部断层。
- 低饱和区域减少 HSL 权重，避免灰墙、白衣、皮肤高光被染色。
- 红色区域需要保真，保留蝴蝶结、唇色、小配饰的识别度，避免被整体低饱和完全压没。
- 黑发和深色区域需要保护暗部，不因褪色/雾感过强而失去头发质感。
- 腮红强度应在所有预设底色下保持可感知变化，尤其是黑白、水色、灰粉等低饱和滤镜不能把强弱调节完全压平。
- 腮红边缘需要柔和羽化，强化后仍应像肤色里透出的红晕，不应出现硬边或明显贴片感。
- 蕾粉等粉色预设需要额外抑制肤色/唇色候选区域的低饱和青绿色偏移，避免鼻翼和唇边出现绿色点状色块。

## 验收标准
- 原图、蕾粉、水色、灰粉、黑白预设都有明确默认参数和审美说明。
- 各滤镜在自拍、人像半身、室内暗光、白墙背景、红色配饰、黑发照片上都不出现明显色块。
- HSL 可调范围足够产生可见效果，但不会轻易破坏脸部、手部和低饱和区域。
- 腮红强弱滑杆在不同底色滤镜下都有明显视觉差异，并保持柔和融合，不出现硬边色块。
- 移动端和 PC 端在同一张图、同一参数下滤镜结果一致。
- 整体风格保持低饱和、病弱、冷调、少女感，而不是高饱和甜美或重口复古。

## 已做修改
- 从 `tasks/filter-render-performance.md` 迁移滤镜参数、HSL 色块、PC/移动端效果一致性相关内容。
- 从 `tasks/hsl-color-safety.md` 迁移 HSL 范围、肤色保护、低饱和保护和预设稳定性相关内容。
- 腮红增加最终阶段的柔和显色叠染，避免被黑白、水色、灰粉、雾感、叠色和暗部保护压平。
- HSL 默认激活通道从绿色改为综合色，保证打开 HSL 后直接拖动滑杆会产生全局可见调色效果。
- 腮红权重曲线收窄中心实色区并扩大淡出范围，区域预览渐变同步增强羽化。
- PC 端滑杆输入改为 requestAnimationFrame 合并提交，拖动中降低预览像素量，综合色 HSL 增强最低可见权重，改善“无效且卡顿”的体验。
- 增加肤色/唇色候选区域的低饱和青绿色色块修正，将零星绿点拉回原图肤色或粉色方向。
- 修复绿色色块修正中的变量作用域错误，避免触发修正时中断滤镜应用。
- 预览渲染改为离屏 canvas 完整绘制后一次性提交到可见 canvas，保留上一帧直到新滤镜帧完成，减少灰色闪烁并保持实时反馈。
- 滤镜滑杆拖动时取消低清轻量预览比例，并让黑发/暗部保护在拖动中也执行，保证拖动中的画面和松手后的画面使用同一套真实滤镜效果。
- 叠加主色拾色器改为首次实际变色时记录一次历史，变色过程只实时更新滤镜，结束时不再重复入栈，保证点击返回能回到改色前。
- 叠加强度联动回退范围补齐 HSL 全通道；黑白、水色、灰粉等强 HSL 预设在强度降低时会同步撤回去饱和/偏色，而不是只撤回基础参数。
- 滑杆首次输入改为立即提交，避免 beginStep 触发重渲染后把真正的强度联动更新延后一帧导致只改滑杆自身。
- 腮红区域权重改为高斯柔化尾巴，实际淡出范围扩大到约 `1.96` 倍半径；预览层同步扩大到 `1.62` 倍半径并降低边缘透明度，减少贴片硬边。
- 叠加强度调整不再重置用户手动选择的叠加主色；只按强度缩放预设数值参数。
- 移除肤色保真、红色保真面板项和滤镜管线逻辑，避免它们抵消 HSL 与预设调色。
- HSL 分区权重降低低饱和门槛，并在预设低饱和场景保留最低可见作用，改善预设下 HSL 调节无效或过弱。
- 蕾粉预设改为更高亮、更粉紫、更柔雾的参数；水色预设改为更冰蓝、低饱和、白灰高亮的参数。
- 蕾粉与水色初始预设强度整体下调约 30%，保留参考色调方向但降低默认冲击度。
- 灰粉、黑白预设提高基础饱和度、肤色提白和腮红强度，并放松肤色/红粉 HSL 去饱和，保留低饱和滤镜下的肤色与腮红。
- 腮红最终叠染加入低饱和补偿：全局饱和度越低、master HSL 去饱和越强，腮红显色和提亮会轻微增强。

## 修改文件
- `src/app.js`
- `tasks/filter-parameter-tuning.md`
- `tasks/filter-render-performance.md`
- `tasks/hsl-color-safety.md`
- `TASKS.md`

## 验证结果
- 已复查任务边界：本任务只包含滤镜效果、参数审美和色彩安全，不包含滑杆性能、导出、文字、贴纸和腮红选区交互。
- `node --check src/app.js` 通过。
- 已在本地浏览器刷新 HSL 面板，确认默认选中第一颗综合色而不是绿色。
- 已在本地浏览器刷新确认 HSL 面板可打开；当前浏览器未上传图片，色彩变化需用户在实图上复测。
- `node --check src/app.js` 通过；已刷新本地页面确认无新的应用脚本错误。
- `node --check src/app.js` 通过；已在浏览器读取当前页面控制台，无新增应用脚本错误。
- `node --check src/app.js` 通过；代码复查确认 `isSliderDragging` 不再参与预览降采样，滤镜拖动中不再跳过黑发/暗部保护。
- 已用本机 Chrome + Playwright 上传样图并拖动滤镜滑杆验证：拖动中 canvas 为 `508×1100`，松手后仍为 `508×1100`，没有切换到低清预览尺寸；拖动中截图与松手后截图尺寸一致，像素 RMS 差异为 `0`。
- 已用本机 Chrome + Playwright 验证叠加主色撤销：从 `#efd4e6` 改为 `#123456` 后点击返回，颜色恢复为 `#efd4e6`，无应用脚本错误。
- `node --check src/app.js` 通过；代码复查确认叠加强度会调用 `blendPresetFiltersByStrength()` 同步缩放基础、人像和 HSL 参数。
- `node --check src/app.js` 通过；代码复查确认腮红实际权重和预览渐变均已扩大羽化半径。
- `node --check src/app.js` 通过；代码搜索确认 `skinProtect` / `redPreserve` 已从应用代码移除。
- `node --check src/app.js` 通过；代码复查确认蕾粉和水色的基础、人像、HSL、叠色强度均按约 70% 保留。
- `node --check src/app.js` 通过；代码复查确认灰粉/黑白预设与腮红低饱和补偿已更新。

## 遗留风险
- 具体数值仍需要结合多张样片调参确认。
- 当前任务只定义参数和审美目标，实际性能问题仍由 `tasks/filter-render-performance.md` 处理。
- 腮红选区交互不在本任务内，腮红参数只能约束强度和肤色融合效果。
